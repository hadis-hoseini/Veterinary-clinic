# Use Case Diagram

```mermaid
usecaseDiagram
    actor User
    actor Admin

    User --> (ثبت نام)
    User --> (ورود)
    User --> (مشاهده اطلاعات)

    Admin --> (مدیریت کاربران)
    Admin --> (مدیریت سیستم)
